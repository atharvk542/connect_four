# connect 4

there is no input column handling at all so be nice to it

change between a MinimaxChooser and a HumanChooser in `player_choosers` in `main.py`
or create a new chooser by inheriting the Chooser base class
